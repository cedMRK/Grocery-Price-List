import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<ion-router-outlet></ion-router-outlet>' // Use a router outlet to display routed components
})
export class AppComponent {}
